version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "no preBuild"
    build:
      commands:
        - echo "no build"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []